# RT-Thread building script for component

from building import *

cwd = GetCurrentDir()
src = Glob('*.c')
CPPPATH = [cwd]

group = DefineGroup('VS1003', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
